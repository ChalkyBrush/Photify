

<% if @comment.errors.present? %>
  $("#comment-form-errors").html("<%= escape_javascript(render(:partial => '/pictures/comment_form_errors')) %>");
<% else %>
  $("#comment-form-errors").html("");
  $("#comment-list").html("<%= escape_javascript(render(:partial => '/pictures/comments')) %>");
  $("#new_comment")[0].reset();
<% end %>

$('#comments-list').html("<%= escape_javascript(render(:partial => '/pictures/comments')) %>");
$('textarea').val("");
$('.comment_content').first().attr("id","lastcomment")
var allchild = $("#lastcomment").children()
//$("#lastcomment").css({width: "0px", height: "0px"});
$("#lastcomment").children().remove()
//$("body").scrollTo( 0, 800, {queue:true} );
var content = $(document).find("#scrollto");
   $('html, body').stop().animate({
        scrollTop: $(content).offset().top -150
    }, 500);
setTimeout(function(){
      $('#lastcomment').append(allchild);
      setTimeout(function(){
      	$('#lastcomment').animate({backgroundColor:"#F2F2F2"}, 1400);
      },600);	
},900);


